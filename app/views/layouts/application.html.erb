
<head>
  <title>Sport$ | <%= yield(:title) %></title>

</head>
<body>
  <%= debug(params) if Rails.env.development? %>

  <% flash.each do |key, value| %>
    <%= content_tag(:div, value, class: "alert alert-#{key} no-margin") %>
    <% end %>
<%= yield %>

</body>

